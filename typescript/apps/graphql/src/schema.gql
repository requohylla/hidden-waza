# ------------------------------------------------------
# THIS FILE IS MANAGED BY THE TEAM (DO NOT MODIFY BY CODEGEN)
# ------------------------------------------------------

type User {
  id: ID!
  username: String!
  email: String!
}

type Resume {
  userId: ID!
  title: String!
  summary: String!
  skills: [Skill!]!
  experiences: [Experience!]!
}

type Skill {
  type: String!
  masterId: ID!
  level: String!
  years: Int!
}

type Experience {
  company: String!
  position: String!
  startDate: String!
  endDate: String!
  description: String!
  portfolioUrl: String!
}

type Language {
  id: ID!
  name: String!
}

type OS {
  id: ID!
  name: String!
}

type Tool {
  id: ID!
  name: String!
}

type LoginResponse {
  id: ID!
  username: String!
  email: String!
}

input LoginInput {
  email: String!
  password: String!
}

input RegisterInput {
  username: String!
  email: String!
  password: String!
}

input ResumeInput {
  title: String!
  summary: String!
  skills: [SkillInput!]!
  experiences: [ExperienceInput!]!
}

input SkillInput {
  type: String!
  masterId: ID!
  level: String!
  years: Int!
}

input ExperienceInput {
  company: String!
  position: String!
  startDate: String!
  endDate: String!
  description: String!
  portfolioUrl: String!
}

type Query {
  me: User
  resumes: [Resume!]!
  skills: [Skill!]!
  languages: [Language!]!
  osList: [OS!]!
  tools: [Tool!]!
}

type Mutation {
  login(input: LoginInput!): LoginResponse!
  register(input: RegisterInput!): User!
  createResume(input: ResumeInput!): Resume!
  updateResume(userId: ID!, input: ResumeInput!): Resume!
  deleteResume(userId: ID!): Boolean!
}